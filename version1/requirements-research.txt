# Optional research extras that are not required to run the current local UI or Docker app.
# Install these only when you need exploratory workflows beyond the current production path.
matplotlib==3.6.2
biomart==0.9.0
deepchem==2.8.0

# Note: pysam currently does not install cleanly on this Windows setup, so it stays optional.
pysam==0.23.1
